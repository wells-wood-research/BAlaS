version: '2'
services:
  web:
    build:
      context: .
      dockerfile: ./web/Dockerfile
    ports:
      - "3803:80"
    volumes:
      - ./web:/app
    depends_on:
      - db
    restart: on-failure
  db:
    image: mongo
